module ifelse

def test
    x = 10
    y = 11
    z = if x < y
        True
    else
        False
    z # True

__END__

-module('ifelse').

test()->
    X = 10,
    Y = 11,
    Z = if
        X < Y ->
            true;
        _ ->
            false
    end,
    Z.
